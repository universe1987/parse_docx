# extract formatted data from docx files and generate a csv file
  1. install python docx
    `pip install python-docx`
  2. put all docx files under data directory
  3. run transform.py in sr directory
  
